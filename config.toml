# The URL the site will be built for
base_url = "https://blog.cronbox.pro"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "tinyblog"

title = "Cronbox Blog"
description = "О технологиях, подходах и решении проблем простым языком"

taxonomies = [
 {name = "tags", rss = true},
 {name = "categories", paginate_by = 5, rss = true},  # 5 items per page for a term
 {name = "authors"} # Basic definition: no RSS or pagination
]

[extra]
# Put all your custom variables here
description = "lelelele"
website="cronbox.pro"
twitter="mental_foreplay"
email = "eugene@cronbox.pro"

open_graph_url = "https://blog.cronbox.pro"
open_graph_type = "blog"
open_graph_title = "Блог Cronbox"
open_graph_description = "Блог о мониторинге, cron"
open_graph_image = ""
